rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // For local demo emulator, allow all operations
      allow read, write: if true;
    }
  }
}
